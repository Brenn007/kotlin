name: CI

on:
  push:
  pull_request:

jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Run shared JVM tests
        run: ./gradlew :shared:jvmTest
        env:
          WEATHER_API_KEY: ${{ secrets.WEATHER_API_KEY }}

      - name: Build Android debug APK
        run: ./gradlew :androidApp:assembleDebug
        env:
          WEATHER_API_KEY: ${{ secrets.WEATHER_API_KEY }}

      - name: Upload Android debug APK
        uses: actions/upload-artifact@v4
        with:
          name: android-debug-apk
          path: androidApp/build/outputs/apk/debug/*.apk

  desktop-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Package Desktop Linux (.deb)
        run: ./gradlew :desktopApp:packageDeb
        env:
          WEATHER_API_KEY: ${{ secrets.WEATHER_API_KEY }}

      - name: Upload Desktop Linux .deb
        uses: actions/upload-artifact@v4
        with:
          name: desktop-linux-deb
          path: desktopApp/build/compose/binaries/main/deb/*.deb

#  desktop-windows:
#    runs-on: windows-latest
#    steps:
#      - uses: actions/checkout@v4
#
#      - uses: actions/setup-java@v4
#        with:
#          java-version: '17'
#          distribution: 'temurin'
#
#      - name: Setup Gradle
#        uses: gradle/actions/setup-gradle@v4
#
#      - name: Package Desktop Windows (.msi)
#        run: ./gradlew :desktopApp:packageMsi
#        env:
#          WEATHER_API_KEY: ${{ secrets.WEATHER_API_KEY }}
#
#      - name: Upload Desktop Windows .msi
#        uses: actions/upload-artifact@v4
#        with:
#          name: desktop-windows-msi
#          path: desktopApp/build/compose/binaries/main/msi/*.msi

#  ios:
#    runs-on: macos-latest
#    steps:
#      - uses: actions/checkout@v4
#
#      - uses: actions/setup-java@v4
#        with:
#          java-version: '17'
#          distribution: 'temurin'
#
#      - name: Setup Gradle
#        uses: gradle/actions/setup-gradle@v4
#
#      - name: Build iOS XCFramework
#        run: ./gradlew :shared:assembleXCFramework
#        env:
#          WEATHER_API_KEY: ${{ secrets.WEATHER_API_KEY }}
